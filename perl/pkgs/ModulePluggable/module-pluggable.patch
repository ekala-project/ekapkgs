diff -ru Module-Pluggable-5.2/lib/Module/Pluggable/Object.pm Module-Pluggable-5.2-new/lib/Module/Pluggable/Object.pm
--- Module-Pluggable-5.2/lib/Module/Pluggable/Object.pm	2015-08-05 14:32:15.000000000 +0000
+++ Module-Pluggable-5.2-new/lib/Module/Pluggable/Object.pm	2025-01-01 00:00:00.000000000 +0000
@@ -195,7 +195,7 @@
         my $sp = catdir($dir, (split /::/, $searchpath));

         # if it doesn't exist or it's not a dir then skip it
-        next unless ( -e $sp && -d _ ); # Use the cached stat the second time
+        next unless ( -d $sp );

         my @files = $self->find_files($sp);

@@ -329,7 +329,7 @@
                              (my $path = $File::Find::name) =~ s#^\\./##;
                              push @files, $path;
                            }
-                      }, $search_path );
+                      }, "$search_path/." );
     }
     #chdir $cwd;
     return @files;
